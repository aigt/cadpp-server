usersByQuery=Select login, password, 'true' from users where login = ?
rolesByQuery=SELECT temptable.login, users_roles.role_id FROM users_roles INNER JOIN (SELECT login, id FROM users WHERE login = ?) temptable ON users_roles.user_id = temptable.id